<%= form_for(@past_log) do |f| %>
  <% if @past_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@past_log.errors.count, "error") %> prohibited this past_log from being saved:</h2>

      <ul>
      <% @past_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Workout %><br>
    <%= f.text_area :Workout %>
  </div>
  <div class="field">
    <%= f.label :Date %><br>
    <%= f.text_area :Date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
